
// chrome/build.gradle

// this is an android library that implements the org.chromium.chrome APIs

dependencies {
  compile project(':chromium:media')
  compile project(':chromium:sync')
  compile project(':chromium:components')
}
